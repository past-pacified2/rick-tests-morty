# `engines` in package.json is a claim; without this line nothing enforces it
engine-strict=true

# Exact versions, no `^` (docs/adr/0006-security.md). `npm install <pkg>` without -E
# would otherwise write a range into package.json and quietly undo the pin.
save-exact=true